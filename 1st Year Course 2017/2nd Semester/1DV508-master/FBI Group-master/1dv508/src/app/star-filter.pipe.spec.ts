import { StarFilterPipe } from './star-filter.pipe';

describe('StarFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StarFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
